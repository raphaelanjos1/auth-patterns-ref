name: 'auth-patterns-ref'

services:
  db:
    image: postgres:17-alpine
    container_name: auth-patterns-ref-db
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - '5432:5432'
    volumes:
      - .data:/var/lib/postgresql/data
